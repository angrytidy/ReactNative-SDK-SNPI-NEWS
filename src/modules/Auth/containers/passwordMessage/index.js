import React from 'react';
import PasswordMessageComponent from './components';


const PasswordMessage = (props) => {
  return <PasswordMessageComponent {...props} />;
};

export default PasswordMessage;
